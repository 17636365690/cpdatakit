# Repository visual assets

`social-preview-v2.jpg` is the upload-ready GitHub social preview: 1280×640 pixels, opaque RGB,
and under GitHub's 1 MB limit. It was created on 2026-08-12 with the built-in image generation tool
as a scientific-educational edit of `social-preview.png`; no research or commercial-solver data was
used.

The image was refined in two passes. The first replaced “CONSISTENCY & PHYSICAL CHECKS” with
“SCHEMA & UNIT CHECKS” and “TRUSTED DATA” with “TRACEABLE DATA”. The final pass used this prompt:

> Edit this repository social-preview image while preserving the exact 2:1 composition, CPDataKit
> title, crystal lattice, pipeline, plots, table, HDF5 block, colors, typography, spacing,
> lighting, and all already accurate text. Make only these precise wording replacements, with
> exact spelling and no extra text: (1) under SOLVER-NEUTRAL replace “Works with any crystal
> plasticity solver” with “Works with explicit tabular inputs”; (2) in the HDF5 checklist replace
> “Efficient” with “Auditable”; (3) in the bottom banner replace “HIGH-QUALITY · REPRODUCIBLE ·
> WELL-DOCUMENTED MATERIALS DATA” with “SCHEMA-VALIDATED · TRACEABLE · WELL-DOCUMENTED MATERIALS
> DATA”. Keep “SCHEMA & UNIT CHECKS” and “TRACEABLE DATA” unchanged. Do not imply physical
> certification, universal solver compatibility, performance, or measured quality. Do not add a
> watermark.

The generated edit was resized to the recommended dimensions and exported as a quality-92
progressive JPEG for upload. The version suffix is intentional: it preserves the original asset
and makes the wording correction auditable.
